import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  title: {
    fontSize: 36,
    fontWeight: "600"
  },
  container: {
    paddingHorizontal: 8
  },
  flex: {
    flex: 1
  },
  loadingContainer: {
    justifyContent: "center",
    alignItems: "center"
  }
})
